{
  "name": "weather-api",
  "version": "0.0.0",
  "main": "dist/index.js",
  "devDependencies": {
    "wrangler": "^2.9.1"
  },
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler publish --dry-run --outdir=./dist/",
    "deploy": "wrangler publish -e production"
  }
}
